{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "versionInterval.json.schema",
    "title": "An interval of versions",
    "description": "",

    "type": "object",

    "properties": {
        "versionBegin": {"type": "string"},
        "versionEnd": {"type": "string"},
        "versionExclusiveBegin": {"type": "boolean"}, 
        "versionExclusiveEnd": {"type": "boolean"}
    },

    "required": ["versionBegin", "versionEnd"],
    
    "additionalProperties": false
}
