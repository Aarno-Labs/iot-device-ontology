{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "event-wraper.schema.json",
    "title": "Event Wrapper",
    "description": "Event in HIKE Management API",
    "type": "object",

    "required": ["op_id", "event_type", "event_data"],

    "properties": {
        "op_id": {
            "type": "string"
        },
        "event_type": {
            "type": "string",
            "pattern": "^0x[a-fA-F0-9]$"
        },
        "event_data": {
            "type": "string"
        }
    },

    "additionalProperties": false
}
